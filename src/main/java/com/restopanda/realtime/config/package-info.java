/**
 * Spring wiring for the realtime-gateway: security chains (the deny-by-default
 * fallback plus the stream/ticket transport rules) and the gateway's
 * configuration properties.
 */
package com.restopanda.realtime.config;
